/**
 * @mineco/protocol — wire contract between SwiftUI thin client and Deno core.
 *
 * Re-export surface kept intentionally narrow: the JSON-RPC envelope, error
 * codes, method-name tables, and shared domain primitives.
 */
export * from './schema.ts';
