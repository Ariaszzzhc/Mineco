# electron-builder collects a flat node_modules to assemble the app; pnpm's
# default symlinked layout trips it up. A hoisted linker gives it the flat tree
# it expects. (Changing this requires a fresh `pnpm install`.)
node-linker=hoisted
